{
  "name": "try-travis",
  "version": "1.0.0",
  "description": "Try out travis build",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage",
    "build": "echo \"Error: no build specified\" && exit 0",
    "build:dist": "echo \"Error: no build dist specified\" && exit 0",
    "build:site": "echo \"Error: no build site specified\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/abhijit945/try-travis.git"
  },
  "author": "Abhijit Rao",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/abhijit945/try-travis/issues"
  },
  "devDependencies": {
    "jest": "^24.5.0"
  },
  "homepage": "https://github.com/abhijit945/try-travis#readme"
}
